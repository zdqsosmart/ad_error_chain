def flatten_dict(d, parent_key='', sep='_'):
    """用于压平字典对象"""
    #         for k in note.keys():
    #             if isinstance(note[k], str):
    #                 message_columns.add((k, ))
    #             if isinstance(note[k], dict):
    #                 for kk in note[k]:
    #                     message_columns.add((k,kk,type(note[k][kk])))
    # for i in message_columns:
    #     print(i)

    # print(message_columns)

    #     for note in notes:
    #         item_ = item.copy()
    #
    #         data.append(item_)
    pass